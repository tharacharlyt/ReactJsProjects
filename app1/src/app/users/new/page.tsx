import RegisterForm from "@/features/users/components/register";

export default function NewUserPage() {
    return (
        <main>
            <h1>Add user</h1>
            <RegisterForm/>
        </main>
    );
}